[project]
name = "boston-geomap"
version = "0.2.0"
description = "GeoGrid-based hotspot mapping for missing species using SOS API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "requests>=2.31.0",
  "flask",
  "flask-cors",
]

[tool.pytest.ini_options]
pythonpath = ["."]